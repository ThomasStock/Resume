# Product

<!-- impeccable:product-schema 1 -->

All facts below were inferred from repository evidence (`CLAUDE.md`, `README.md`, `src/data/resume.ts`, build and test scripts) during an autonomous session; the init interview was not held. Each section is marked *(inferred)*. Correct anything that is wrong.

## Platform

web

## Users

*(inferred)* Primary: recruiters, hiring managers and engineering leads at companies or agencies evaluating Thomas Stock for a freelance lead front-end engagement. They arrive from LinkedIn, GitHub, a shared link or a search, spend one to three minutes, and want to answer "is this person worth a call?" Secondary: people who receive the PDF by email and read it printed or on a phone; LLM agents and crawlers reading `/resume.json`, `/resume.md` and `/llms.txt`.

## Product Purpose

*(inferred)* A single-source resume for Thomas Stock, freelance Lead Front-end Engineer (Stackhouse BV, Belgium, remote). One typed data file renders the web page, a one-page A4 PDF, an Open Graph image, JSON Resume, Markdown and llms.txt. Success: a reader understands role, seniority, current focus and track record within the first screen, and can reach Thomas or download the PDF without friction.

## Positioning

*(inferred)* Twenty years of shipping web software from .NET to React, with a current focus on making codebases and teams ready for AI-assisted development. The resume itself demonstrates the claim: one source of truth, generated artifacts, tests, and a machine-readable surface for agents.

## Operating Context

*(inferred)* Read on desktop and phone, light and dark OS themes; printed or attached as PDF, always one A4 page. Deployed at https://www.stackhouse.be as a static Astro site. Content is edited in `src/data/resume.ts`; every output is regenerated by `npm run build` and checked by `npm test`.

## Capabilities and Constraints

*(inferred, from CLAUDE.md)*
- All copy lives in `src/data/resume.ts`; components carry no resume copy.
- Years of experience are derived from `person.startYear`, never hardcoded.
- The PDF must stay exactly one A4 page; type never below 8.5pt in print. Print CSS sits next to screen CSS in each component; all sizes in rem, print scales via the root font size.
- Light default, dark via `prefers-color-scheme`, print always light.
- Fonts self-hosted via Fontsource (Inter Variable, Instrument Serif). No third-party CDNs.
- No client-side JavaScript except the copy-to-clipboard button.
- Undecided: `person.availability` (rendered only when set); several bullets carry `TODO(thomas)` markers for real numbers.

## Brand Commitments

*(inferred)* Name: Thomas Stock. Business: Stackhouse BV, domain stackhouse.be. Voice: first person, plain, specific, no marketing adjectives. Portrait: `src/assets/thomas.jpeg`.

## Evidence on Hand

*(inferred)* Real career data in `src/data/resume.ts` (roles 2006 to present, education, languages, VAT number). No testimonials, logos or metrics beyond what the data file states; do not invent any.

## Product Principles

*(inferred)*
1. One source of truth; design never forks content.
2. Scannable first: a reader finds role, focus and contact in seconds.
3. Every output stays honest to the data: the web page, PDF and machine formats say the same thing.
4. Restraint over decoration; the work and the words carry the page.
5. Durable: no dependencies on third parties at runtime, no hardcoded time-sensitive values.

## Accessibility & Inclusion

*(inferred)* WCAG AA as the floor: contrast, keyboard focus, semantic landmarks and headings, alt text. The page must remain readable at 200% zoom and in both color schemes.
