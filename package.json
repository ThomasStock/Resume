{
  "name": "thomas-stock-resume",
  "type": "module",
  "version": "3.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/render-artifacts.mjs",
    "build:web": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "pdf": "node scripts/render-artifacts.mjs"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/instrument-serif": "^5.3.0",
    "astro": "^7.3.1",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "pdf-lib": "^1.17.1",
    "playwright": "^1.63.0",
    "typescript": "^6.0.3"
  },
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  }
}
